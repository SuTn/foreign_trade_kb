feat: 客户重新匹配 + 分层历史前端入口

A 客户重新匹配 (纠正误合并):
- 新增 POST /customers/{id}/remap: 把会话重新映射到另一客户并置 confirmed=1
- 匹配区块加「重新匹配」按钮, 展开客户下拉选择合并目标
- workspace_side 附带客户列表 (排除当前客户)

B 分层历史前端入口:
- 画像 tab 新增「分层历史」时间线区块
- 新增 tier_history_list.html 时间线模板
- /api/tiering/history/{id} 支持 htmx 请求返回渲染片段
- workspace_side 直接渲染历史 (避免 htmx load 触发不可靠)

review 修复:
- 补 tier_history_list.html 末尾换行

测试: 329 passed (新增 3 个测试: remap 成功/目标不存在/历史时间线)