feat: max_tokens 可配置 + worker 双线程并发

C max_tokens 可配置:
- config 新增 llm_max_tokens=2048 (回复生成默认, 长回复不被截断)
- pipeline.run 支持 max_tokens 参数, 缺省用 settings.llm_max_tokens
- 各调用点按场景传合适值: 分层 256 / 同义判断 16 / wiki 抽取 512 /
  摘要 2048 / 分析·跟进·画像 1024

D worker 双线程并发:
- worker 拆为回复线程 + 后台线程 (分层/摘要), 回复永不阻塞于分层/摘要
- 各线程独立 SQLite 连接 (WAL + busy_timeout 兜底并发写)
- 分层任务去掉回复优先检查 (回复由独立线程处理)
- app.py 改用 start_worker 启动双线程

review 修复:
- 补 config.py / summarizer.py / test_worker_summary.py 末尾换行

测试: 329 passed (更新分层/摘要 worker 测试适配新结构)