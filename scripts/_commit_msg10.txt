feat: SQLite 每线程连接 + 采集器归一化重构 + 画像双轨统一

E SQLite 连接重建 (A1):
- _store 改用 threading.local 每线程独立连接, 避免多线程共享连接
- 保留 app.state.sqlite_store 供测试/关闭清理引用

F 采集器 _merge_idb_dom 重构 (A7):
- 拆为模块级小函数: _build_idb_index / _dom_sender_name / _build_lid_by_phone /
  _jid_forms / _resolve_chat / _resolve_phone_chat / _resolve_chat_name / _resolve_sender_name
- 新增 5 个归一化函数单测

G 画像 EAV + customers 双轨 (A4):
- 新增 store.sync_customer_column: 抽取的 company/country 同步到 customers 固定列
- 使 search_customers (查固定列) 能命中 company/country
- 新增同步测试

测试: 335 passed (新增 5 个 scanner 单测 + 1 个 extractor 同步测试)