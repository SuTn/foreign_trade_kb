feat: 客户分析结构化 + 工作台批量分层入口

A. 客户分析结构化:
- analyze_customer 改为输出结构化 JSON (兴趣点/活跃度/跟进建议/总结)
- 新增 _parse_analysis 容错解析 (整体 JSON → 提取块 → 回退文本)
- analysis.html 从裸 <pre> 改为结构化卡片
- 路由错误处理改为结构化 dict

B. 工作台批量分层入口:
- 左栏新增"批量分层"按钮 + 进度条
- 点击 POST /api/tiering/analyze → 轮询 /api/tiering/status → 完成后刷新客户列表
- 新增 GET /workspace/customers 片段端点 (供分层后局部刷新)
- tiering_status 响应补充 total 字段

review 修复:
- refreshCustomers 改用 /workspace/customers 片段 (原 /workspace 会嵌套整页)
- 分层 IIFE 加 DOMContentLoaded 等待 (原在 head 加载时 btn 为 null)
- JS 进度解析适配 progress 为整数 + total 字段
- 补末尾换行

测试: 321 passed (新增 3 个分析解析测试 + 1 个片段端点测试)